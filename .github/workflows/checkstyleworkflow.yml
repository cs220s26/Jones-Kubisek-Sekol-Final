name: CI Checkstyle

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  CheckstyleJob:
    runs-on: ubuntu-latest

    steps:
      - run: mvn checkstyle:check
        working-directory: ./Jones-Kubisek-Sekol-Final
      - name: Checkout
        uses: actions/checkout@v6.0.2
