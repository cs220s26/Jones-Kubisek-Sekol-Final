name: CI Checkstyle

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  CheckstyleJob:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6.0.2
    
      - run: mvn checkstyle:check
